# STATUS

Last consolidation: (never)

## Active work
| Date | Area / files | Task & state (≤3 lines) | Signature |
|------|--------------|-------------------------|-----------|
| 2026-07-24 | bani, amadinda/ricercar/svara CSS | Fixed bani: voice envelope never reopened after each line's cadence (missing isFirstOfLine flag), so only the 2nd of ~12 lines ever sang — rest was breath/consonant noise only. Fixed amadinda/ricercar/svara: stray `prefers-color-scheme:light` CSS silently swapped their dark theme for an off-white one on light-mode browsers, unlike every other machine. Done, verified via offline WAV render (bani) and code diff (CSS). | Signed: Claude Code \| Sonnet 5 \| medium |

## Shared-file touches
(list file + what changed + signature)

## Known issues
(one line each: what, where, date noticed, signature)

## Archive
(completed entries moved here during consolidation — one line each)
