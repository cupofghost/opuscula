{
  "name": "opvscvla-dev",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Dev-only tooling for OPVSCVLA (verify harness + drift checker). Never shipped with the machines; the machines stay one-file, zero-dependency.",
  "scripts": {
    "verify": "node verify.mjs",
    "check": "node check.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.0"
  }
}
